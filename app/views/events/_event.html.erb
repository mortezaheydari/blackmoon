            <% reload ||= false %>
          <% style_id = class_name + event.id.to_s %>
          <%= "<article class=\"activityBox\" id=\"event #{style_id} \">".html_safe unless reload == true %>
            <header class="activityHeader">
              <%= link_to image_tag("bg.png", class: "activityImage"), event_path(event) %>
<!--               <a href="#">
                <%= image_tag "bg.png", class: "activityImage" %>
              </a> -->
              <h5 class="activityTitle text-left">
                <%= link_to event.title, event_path(event) %>
              </h5>
              <p class="text-left"><strong>Originator:</strong> <%= event.creator.name %><br><%= event.date_and_time.strftime("%A, %B %d, %Y") %></p>
              <div class="<%= event.category %>"></div>
            </header>

            <div class="activityBody text-center">
              <ul class="block-grid three-up">
                <li><%= image_tag "1.png" %></li>
                <li><%= toggle_like_card_button(event, current_user, {style_id: style_id, class_name: class_name}) if signed_in? %> <span id="likecount<%= event.id %>" class="small-text"><%= event.flaggings.with_flag(:like).count if signed_in?  %> </span> </li>
                <li><div class="featureStar"></div> </li>
              </ul>

            </div>
          <%= "</article>".html_safe unless reload == true %>