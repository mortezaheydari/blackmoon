

<% @activities.each do |activity| %>


	<%= m = render_activity(activity, render_for: "notification") %>
	<% unless m.nil? %>
	 
	  <%= actvity.created_at %>
	  <%# if activity.key == "invitation.create" %>
	  <%= time = activity.trackable %>

	<%- end %>

<% end %>