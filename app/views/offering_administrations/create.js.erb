$("span#makeAdmin<%= person.id %>").html("<%= escape_javascript(render('users/remove_admin', person: person,  this: this)) %>")

<% if this.administrators.count > 1 %>
    $("span#makeAdmin<%= this.creator.id %>").html("<%= escape_javascript(render('users/remove_admin', person: person,  this: this)) %>")
<% end -%>