<!-- act_administration.destroy -->

<!-- 1. feed -->

<!-- 2. log -->
	<% elsif render_for == "log" %>
		"<%= link_to activity.recipient.profile.name, activity.recipient %>" was removed from administration board.

<!-- 3. notification -->
	<% elsif render_for == "notification" %>
		<% if activity.trackable %>
			"you no longer administrate "<%= link_to activity.trackable.title, activity.trackable %>".

		<%- end %>

<!-- 4. public -->

<!-- 5. else -->

	<% else %>
		admin removed.

	<%- end %>