<!-- act_membership.destroy -->

<!-- 1. feed -->

<!-- 2. log -->
	<% elsif render_for == "log" %>
		"<%= link_to activity.recipient.profile.name, activity.recipient %>" is no longer a member.

<!-- 3. notification -->
	<% elsif render_for == "notification" %>
		<% if activity.trackable %>
			you are no longer a member of "<%= link_to activity.trackable.title, activity.trackable %>".

		<%- end %>

<!-- 4. public -->

<!-- 5. else -->

	<% else %>
		member removed.

	<%- end %>