<%#  offering_individual_participation.create %>

<%#  1. feed %>
    <% if @render_for.nil? || @render_for == "feed" %>
   		<% if activity.owner && activity.trackable %> 
            <%= link_to activity.owner.title, activity.owner %> joined <%= link_to activity.trackable.title, activity.trackable %>.
            
        <%- end %>

<%#  2. log %>
    <% elsif @render_for == "log" %>
        <% if activity.owner %>		
            <%= link_to activity.owner.title, activity.owner %> joined.

        <%- end %>

<%#  3. notification %>
    <% elsif @render_for == "notification" %>

<%#  4. public %>
    <% elsif @render_for == "public" %>

<%#  5. else %>
    <% else %>
        new user joined.

    <%- end %>