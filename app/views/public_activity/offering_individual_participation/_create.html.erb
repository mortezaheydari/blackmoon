<!-- offering_individual_participation.create -->

<!-- 1. Offering activity -->
	<% if ["Game", "Event"].include? activity.recipient_type  %>

		"new participator "<%= link_to activity.owner.title, activity.owner %>" added.

<!-- 2. user notification -->
	
	<% elsif activity.recipient_type == "User" %>

		<% if activity.trackable %>
			"you have been added to participate in "<%= link_to activity.trackable.title, activity.trackable %>".
		<% else %>
			you were added to participate in offering, which has since been removed.
		<%- end %>

<!-- 3. other -->	

	<% else %>
		new participator added.
		
	<%- end %>
