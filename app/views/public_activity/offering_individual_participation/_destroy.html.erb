<%#  offering_individual_participation.destroy %>

<%#  1. feed %>
    <% if @render_for.nil? || @render_for == "feed" %>

<%#  2. log %>
    <% elsif @render_for == "log" %>
        <% if activity.owner %>		
            <%= link_to activity.owner.title, activity.owner %> no longer participates.

        <% else %>
            a participator was removed.

        <%- end %>

<%#  3. notification %>
    <% elsif @render_for == "notification" %>

<%#  4. public %>
    <% elsif @render_for == "public" %>

<%#  5. else %>
    <% else %>
        participator left.

    <%- end %>