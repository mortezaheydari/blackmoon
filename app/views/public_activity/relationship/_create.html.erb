<!-- relationship.create -->

<!-- 1. user notification -->

	<% if activity.recipient_type == "User" %>

		<% if activity.trackable %>
			you have a new follower: "<%= link_to activity.trackable.title, activity.trackable %>".
		<% else %>
			you had a new follower, which has since been removed.
		<%- end %>

<!-- 2. other -->		

	<% else %>
		a new game is available.

	<%- end %>