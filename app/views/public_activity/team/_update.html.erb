<%# team.update %>

<%# 1. feed %>
	<% if render_for == "feed" %>

<%# 2. log %>
	<% elsif render_for == "log" %>
		<% if activity.owner && activity.trackable %>
			"Updated by <%= link_to activity.owner.name, activity.owner %>".

		<%- end %>	

<%# 3. notification %>
	<% elsif render_for == "notification" %>

<%# 4. public %>
	<% elsif render_for == "public" %>

<%# 5. else %>
	<% else %>
		Updated.

	<%- end %>
