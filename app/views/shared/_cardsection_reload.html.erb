<!-- remeber the doppelganger -->
<%= content_tag("div", "", class: "cardsection #{class_name}", remote: :true) do %>
    <%  @likes.each do |individual| %>
            <%= render 'users/usercards', person: individual.flagger, event_id: event_id %>
    <% end %>
<% end %>