<article class="userCard">
  <header class="userCardHeader">
     <%= link_to  gravatar_for(person, gravatar_class: "userCardImage left"), user_path(person)%>
    <h5 class="userCardTitle text-left">
      <%= link_to person.name, user_path(person) %>
    </h5>
  </header>

  <div class="userCardLevel">
    <p>Level: First Boomer</p>
  </div>

  <div class="userCardRecent text-left">
    <p>Recent Activities</p>
    <div></div>
    <div></div>
    <div></div>
    <div></div>
    <% if user_is_admin?(event) %>
        <% if !user_is_admin?(event,person) %>
            <%= render 'users/make_admin', person: person,  event_id: event.id %>
        <% elsif user_is_admin?(event, person) && (user_created_this?(event) or current_user?(person)) && event.administrators.count > 1 %>
            <%= render 'users/remove_admin', person: person,  event_id: event.id %>
        <% end %>
    <% end %>
  </div>
</article>